
local VelarisUI = loadstring(game:HttpGet("https://raw.githubusercontent.com/nhfudzfsrzggt/brigida/refs/heads/main/dist/main.lua", true))()

_G.EngineReady = false
task.spawn(function()
    local RS = game:GetService("ReplicatedStorage")
    local remote = RS:FindFirstChild("menuToggleRequest") or RS:WaitForChild("menuToggleRequest", 5)
    if remote then remote:FireServer() task.wait(0.5) end
    local mainMenu = game:GetService("Players").LocalPlayer.PlayerGui:FindFirstChild("mainMenuSystem")
    if mainMenu then mainMenu:Destroy() end
    _G.EngineReady = true
end)


local HttpService = game:GetService("HttpService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local TweenService = game:GetService("TweenService")
local Players = game:GetService("Players")
local LP = Players.LocalPlayer -- Kita pakai LP untuk LocalPlayer
local RunService = game:GetService("RunService")
local PlayerData = LP:WaitForChild("PlayerData") -- DIPERBAIKI: Dari Player jadi LP
local UserInputService = game:GetService("UserInputService")
local CourierSettings = require(ReplicatedStorage:WaitForChild("Delivery System"):WaitForChild("Settings"))


-- // CFRAMES
local SupplyCF = CFrame.new(-5116.78418, 5.78931046, -670.858887)
local MachineCF = CFrame.new(-4997.1665, 1.58353043, -795.047607)
local RegisterCF = CFrame.new(-4994.06934, 1.30402756, -760.247437)
local StartJobCF = CFrame.new(-4989.80078, 5.30382967, -715.013062)
local TAKE_BOX_CFRAME = CFrame.new(-5105.61182, 4.48948574, -3758.98267)
local TAKE_PROMPT = workspace:WaitForChild("Livrason"):WaitForChild("Take1"):WaitForChild("Take"):WaitForChild("ProximityPrompt")

-- // CONFIG
_G.AutofarmCourier = false 
_G.CourierSpeed = 230  
_G.CycleCount = 0
_G.Antiafk = false
_G.MaxForce = 200
_G.Increment = 50
_G.Interval = 0.5
_G.BoostEnabled = false
_G.StraightBoost = false
_G.HideName = false
_G.HideChar = false
_G.AutoWebhook = false
_G.WebhookURL = "" 
_G.TotalEarning = 0
_G.CycleCount = 0
_G.StartTime = os.time()
LastActivity = tick()
local lastMoney = PlayerData.RPValue.Value
local pendingIncome = 0
local isRunning = false
local cooldownTime = 60
local lastBoostTime = 0

-- Timer 3 Jam (Anti-Reset)
if not _G.StartTime then
    _G.StartTime = os.time()
end

WaktuMulai = _G.StartTime
LastActivity = tick()
local WaktuKosong = nil


-- [[  UTILITY FUNCTIONS ]]
local function RejoinServer()
    local TeleportService = game:GetService("TeleportService")
    local Players = game:GetService("Players")
    local RS = game:GetService("ReplicatedStorage")

    -- 1. BYPASS: Mainin Remote sebelum cabut biar server nyatet lo "Safe"
    local remote = RS:FindFirstChild("menuToggleRequest") or RS:WaitForChild("menuToggleRequest", 5)
    if remote then
        remote:FireServer()
    end

    -- 2. TITIP SCRIPT: Biar pas masuk lagi langsung jalan otomatis
    local scriptRaw = [[loadstring(game:HttpGet("https://raw.githubusercontent.com/VoidlineHub/VoidlineHub/main/dds"))()]]
    local teleportFunc = queue_on_teleport or (syn and syn.queue_on_teleport)
    
    if teleportFunc then
        teleportFunc(scriptRaw)
    end

    -- 3. EKSEKUSI REJOIN
    task.wait(0.5)
    TeleportService:TeleportToPlaceInstance(game.PlaceId, game.JobId, Players.LocalPlayer)
end


local function SwitchToCourier()
    local TeamRemote = ReplicatedStorage:FindFirstChild("TeamChangeRequest", true)
    if TeamRemote then
        if not LP.Team or LP.Team.Name ~= "Courier" then
            TeamRemote:FireServer("Courier", 11378976, 0, 0, "Detector")
            task.wait(1.5)
        end
    end
end

local function Tween(targetCFrame)
    local Char = LP.Character
    local Root = Char and Char:FindFirstChild("HumanoidRootPart")
    if not Root then return end

    local distance = (Root.Position - targetCFrame.Position).Magnitude
    local duration = distance / _G.CourierSpeed

    Root.Velocity = Vector3.new(0,0,0)
    Root.RotVelocity = Vector3.new(0,0,0)

    local info = TweenInfo.new(duration, Enum.EasingStyle.Linear)
    local tween = TweenService:Create(Root, info, {CFrame = targetCFrame})

    tween:Play()

    local connection
    connection = game:GetService("RunService").Stepped:Connect(function()
        if tween.PlaybackState == Enum.PlaybackState.Playing then
            Root.Velocity = Vector3.new(0,0,0)
        else
            connection:Disconnect()
        end
    end)

    tween.Completed:Wait()
    Root.Velocity = Vector3.new(0,0,0)
end

local function AutoEquipBox()
    local Char = LP.Character
    if not Char or not Char:FindFirstChild("Humanoid") then return false end
    local held = Char:FindFirstChildOfClass("Tool")
    if held and held.Name:lower() == "box" then return true end
    local bp = LP:FindFirstChild("Backpack")
    if bp then
        for _, item in pairs(bp:GetChildren()) do
            if item:IsA("Tool") and item.Name:lower() == "box" then
                Char.Humanoid:EquipTool(item)
                return true
            end
        end
    end
    return false
end

local function GetActivePoint()
    -- Pastikan CourierSettings sudah terdefinisi di atas script
    for _, folder in ipairs(CourierSettings.Folder.Location:GetChildren()) do
        local block = folder:FindFirstChild("Block")
        local prompt = block and block:FindFirstChildOfClass("ProximityPrompt")
        if prompt and (prompt.Enabled or folder:FindFirstChild("POINT").billboardgui.Enabled) then
            return block, prompt
        end
    end
    return nil, nil
end

-- // 2. FUNGSI AUTO-AVATAR (Narik foto profil user otomatis)
local function getAvatar()
    -- Pastikan pakai LP.UserId agar sinkron dengan Data Binding lu
    return "https://www.roblox.com/headshot-thumbnail/image?userId=" .. LP.UserId .. "&width=420&height=420&format=png"
end

-- // 3. FORMAT RUPIAH
local function formatRP(v)
    local s = string.format("%.0f", v)
    local formatted = s:reverse():gsub("(%d%d%d)", "%1."):reverse():gsub("^%.", "")
    return "RP. " .. formatted
end

-- // 4. RUNNING TIME
local function getRunningTime()
    local diff = os.time() - _G.StartTime
    return string.format("%02d:%02d:%02d", math.floor(diff/3600), math.floor((diff%3600)/60), diff%60)
end

-- // 5. FUNGSI WEBHOOK (VOIDLINE EMBED AUTHOR VERSION)
local function sendWebhook(income, target)
    if _G.WebhookURL == "" or not _G.WebhookURL:find("discord.com") then return end
    
    _G.CycleCount = _G.CycleCount + 1
    _G.TotalEarning = _G.TotalEarning + income
    
    local currentMoney = PlayerData.RPValue.Value
    local http_request = request or http_request or (syn and syn.request) or (fluxus and fluxus.request)

    local embed = {
        ["author"] = {
            ["name"] = "Voidline Webhook",
            ["icon_url"] = getAvatar() -- Foto profil lu (LP)
        },

        ["title"] = "webhook logs",
        ["color"] = 0xFFFFFF,

        ["fields"] = {
            {["name"] = "Username", ["value"] = LP.Name, ["inline"] = false}, -- DIUBAH: Dari Player ke LP
            {["name"] = "Cycle Income", ["value"] = formatRP(income), ["inline"] = false},
            {["name"] = "Target", ["value"] = formatRP(target), ["inline"] = false},
            {["name"] = "Current Money", ["value"] = formatRP(currentMoney) .. " (Est)", ["inline"] = false},
            {["name"] = "Total Earning", ["value"] = formatRP(_G.TotalEarning) .. " (Est)", ["inline"] = false},
            {["name"] = "Cycle Count", ["value"] = tostring(_G.CycleCount), ["inline"] = false},
            {["name"] = "Running Time", ["value"] = getRunningTime(), ["inline"] = false}
        },

        ["image"] = {
            ["url"] = "https://cdn.discordapp.com/attachments/1478884254028140611/1484460670211723304/Gemini_Generated_Image_7og1bq7og1bq7og1.png?ex=69be4f3b&is=69bcfdbb&hm=d179409928b7a1277912aedfca658b94b37a4a99b5f3bb6d39ba9850b1302a6d&"
        },

        ["footer"] = {
            ["text"] = "Powered by Voidline | " .. os.date("%m/%d/%Y %I:%M %p")
        }
    }

    local payload = HttpService:JSONEncode({
        ["username"] = "Voidline Reports", 
        ["embeds"] = {embed}
    })

    if http_request then
        pcall(function()
            http_request({
                Url = _G.WebhookURL,
                Method = "POST",
                Headers = {["Content-Type"] = "application/json"},
                Body = payload
            })
        end)
    end
end

-- // 6. MONEY MONITOR (DAFTAR PERUBAHAN DUIT)
PlayerData.RPValue:GetPropertyChangedSignal("Value"):Connect(function()
    local newMoney = PlayerData.RPValue.Value
    if newMoney > lastMoney then
        pendingIncome = pendingIncome + (newMoney - lastMoney)
        
        if not isRunning then
            isRunning = true
            task.spawn(function()
                while isRunning do
                    task.wait(60)
                    if pendingIncome > 0 and _G.WebhookURL ~= "" then
                        sendWebhook(pendingIncome, 0)
                        pendingIncome = 0
                    end
                    -- Auto stop jika semua farm off
                    if not _G.AutofarmCourier and not _G.AutoFarmBarista then
                        isRunning = false
                    end
                end
            end)
        end
    end
    lastMoney = newMoney
end)

-- [[ MAIN ENGINE ]]
task.spawn(function()  
    while true do
task.wait(1)
        if os.time() - WaktuMulai >= 3600 then
            RejoinServer()
            break
        end

        -- WAJIB: Cek EngineReady (Cleanup) DAN Toggle ON
        if _G.EngineReady and _G.AutofarmCourier then
            
            SwitchToCourier()

            local Char = LP.Character or LP.CharacterAdded:Wait()
            local Hum = Char:FindFirstChild("Humanoid")
            local Root = Char:FindFirstChild("HumanoidRootPart")

            if Hum and Root then
                -- [[ SIT BYPASS ]]
                if not Hum.Sit then
                    repeat 
                        if not _G.AutofarmCourier then break end
                        Hum.Sit = true
                        task.wait(0.5)
                    until Hum.Sit or not _G.AutofarmCourier
                    task.wait(1) 
                end
                -- PHASE 1: AMBIL PAKET
                if not AutoEquipBox() then
                    -- Tween ke tempat ambil box
                    Tween(TAKE_BOX_CFRAME)
                    task.wait(0.5)
                    
                    if _G.AutofarmCourier and TAKE_PROMPT.Enabled then
                        fireproximityprompt(TAKE_PROMPT)
                        task.wait(1.5)
                    end
                
                -- PHASE 2: ANTER PAKET
                else
                    local TargetBlock, TargetPrompt = GetActivePoint()
                    
                    if TargetBlock and TargetPrompt then
                        task.wait(math.random(0, 1))
                        
                        -- Tween ke tempat antar
                        Tween(TargetBlock.CFrame * CFrame.new(0, 2, 0))
                        task.wait(0.8)
                        
                        AutoEquipBox() 
                        
                        if _G.AutofarmCourier and TargetPrompt.Enabled then
                            fireproximityprompt(TargetPrompt)
                            task.wait(3.5)
                        end
                    end
                end
            end
        end
    end
end)




-- [[ GUI VELARIS ]] --
local Window = VelarisUI:Window({
    Title     = "Drag Drive Simulator",
    Footer    = "By Voidline",
    Color     = "Default",            
    Version   = "1.0",
    Image     = "101833678008843",    
    Size      = UDim2.fromOffset(640, 400),
    ShowUser  = true,                  
    Search    = true,                  
    Animation = true,
    Config = {
        ConfigFolder = "voidlineconfig",
        AutoSaveFile = "Default",
        AutoSave     = true,
        AutoLoad     = true,
    },
})

local InfoTab = Window:AddTab({ Name = "information", Icon = "house" })
local InfoSection = InfoTab:AddSection({ Title = "Information", Open = true })
InfoSection:AddParagraph({
    Title   = "Script Update",
    Content = "•fix bug\n•fix courier 3 jam stuck\n•remove barista farm\n•added webhook\n•added misc tab"
})
local MediaSection = InfoTab:AddSection({ Title = "Media support", Open = true })
MediaSection:AddParagraph({
    Title   = "Support📷",
    Content = "Join Discord Voidline agar dapat informasi update terbaru!"
})

MediaSection:AddButton({
    Title = "Discord link",
    Description = "Salin link Discord ke Clipboard",
    Callback = function()
        setclipboard("https://discord.gg/YrA4wfCC6d")
        -- Notifikasi opsional biar user tau udah ke-copy
        game:GetService("StarterGui"):SetCore("SendNotification", {
            Title = "Voidline Hub",
            Text = "Discord Link copied to clipboard!",
            Duration = 5
        })
    end
})

MediaSection:AddButton({
    Title = "TikTok Account",
    Description = "Follow TikTok owner!",
    Callback = function()
        setclipboard("https://www.tiktok.com/@haloowftd?_r=1&_t=ZS-964QIrS07Hj")
        game:GetService("StarterGui"):SetCore("SendNotification", {
            Title = "Voidline Hub",
            Text = "TikTok Link copied to clipboard!",
            Duration = 5
        })
    end
})

local FarmTab = Window:AddTab({ Name = "AutoFarm", Icon = "house" })
local FarmSection = FarmTab:AddSection({ Title = "courier farm", Open = true })
FarmSection:AddParagraph({
    Title   = "ALERT⚠️",
    Content = "kalau pakai redfinger bisa speed 300\nkalau hp biasa speed 230 (recommend)"
})

FarmSection:AddToggle({
    Title    = "On Autofarm courier",
    Content = "farm nya dah bagus",
    Flag  = "config",
    Default  = false,
    Callback = function(value)
        _G.AutofarmCourier = value
    end
})

FarmSection:AddSlider({
    Title     = "speed farm",
    Flag      = "config slider",  
    Content   = "pakai senormalnya",
    Min       = 0,
    Max       = 270,
    Default   = 230,
    Increment = 1,
    Callback  = function(value)
    _G.CourierSpeed = value
    end
})

local BaristaSection = FarmTab:AddSection({ Title = "Barista Farm", Open = false })
BaristaSection:AddParagraph({
    Title   = "incoming",
    Content = "barista dalam keadaan maintenance"
})

local VehicleTab = Window:AddTab({ Name = "vehicle modif ", Icon = "house" })
local SetingsSection = VehicleTab:AddSection({ Title = "Setting", Open = false })

SetingsSection:AddInput({
    Title    = "speed",
    Flag     = "BoostMaxForce",
    Content  = "Enter value",
    Default  = "200",
    Callback = function(value)
        _G.MaxForce = tonumber(value) or 200
    end
})
SetingsSection:AddInput({
    Title    = "Increment Boost",
    Flag     = "BoostIncrement", 
    Content  = "Enter value (Ex: 50)",
    Default  = "50",
    Callback = function(value)
        _G.Increment = tonumber(value) or 50
    end
})
SetingsSection:AddInput({
    Title    = "Boost Interval",
    Flag     = "BoostInterval",
    Content  = "Enter seconds",
    Default  = "0.5",
    Callback = function(value)
        _G.Interval = tonumber(value) or 0.5
    end
})

local OnSettingSection = VehicleTab:AddSection({ Title = "on setting", Open = true })
-- Toggle buat nyalain Auto Boost
OnSettingSection:AddToggle({
    Title    = "Vehicle Boost",
    Content  = "On/Off Automatic Boost",
    Flag     = "VehicleBoostToggle",
    Default  = false,
    Callback = function(value)
        _G.BoostEnabled = value
    end
})

-- Toggle buat Straight Boost (biar gak terbang)
OnSettingSection:AddToggle({
    Title    = "Straight Boost",
    Content  = "Boost horizontal only",
    Flag     = "StraightBoostToggle",
    Default  = false,
    Callback = function(value)
        _G.StraightBoost = value
    end
})

local WebhookTab = Window:AddTab({ Name = "Webhook ", Icon = "house" })
local WebhookSection = WebhookTab:AddSection({ Title = "Webhook farm ", Open = true })
WebhookSection:AddInput({
    Title    = "webhook",
    Flag     = "webhook",
    Content  = "Enter link webhook",
    Default  = "",
    Callback = function(value)
        _G.WebhookURL = value
    end
})

WebhookSection:AddToggle({
    Title    = "enable webhook",
    Content  = "webhook ngirim setiap 1 menit",
    Flag     = "webhooktogle",
    Default  = false,
    Callback = function(value)
        _G.AutoWebhook = value
    end
})

-- tab misc
local MiscTab = Window:AddTab({ Name = "Misc ", Icon = "house" })
local AvatarSection = MiscTab:AddSection({ Title = "great features ", Open = true })

AvatarSection:AddToggle({
    Title = "Hide All Names",
    Content = "Hide all player nametags",
    Flag = "HideNames",
    Default = false,
    Callback = function(v)
        _G.HideName = v
    end
})

AvatarSection:AddToggle({
    Title = "Hide Character",
    Content = "Remove character model",
    Flag = "HideChar",
    Default = false,
    Callback = function(v)
        _G.HideChar = v
        if not v and LP.Character then 
            LP.Character.Parent = workspace 
        end
    end
})

AvatarSection:AddToggle({
    Title    = "antiafk",
    Content = "biar ga ke kick 20 mnit",
    Flag = "config",
    Default  = false,
    Callback = function(value)
        _G.antiafk = value
    end
})

-- [[ ANTI-AFK ENGINE ]] --
task.spawn(function()
    local VirtualUser = game:GetService("VirtualUser")
    game:GetService("Players").LocalPlayer.Idled:Connect(function()
        if _G.antiafk then
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end
    end)
end)

local GraphSection = MiscTab:AddSection({ Title = "Graphics setting", Open = true })
GraphSection:AddButton({
    Title = "Potato Mode",
    Description = "Remove textures and effects for low-end devices",
    Callback = function()
        for _, v in pairs(game:GetDescendants()) do
            if v:IsA("BasePart") or v:IsA("MeshPart") then
                v.Material = Enum.Material.Plastic
                v.Reflectance = 0
            elseif v:IsA("Decal") or v:IsA("Texture") then
                v:Destroy()
            elseif v:IsA("ParticleEmitter") or v:IsA("Trail") then
                v.Enabled = false
            end
        end
    end
})

GraphSection:AddToggle({
    Title = "Black Screen",
    Content = "Overlay black screen to save GPU/Battery",
    Flag = "BlackScreen",
    Default = false,
    Callback = function(v)
        if typeof(SetBlackScreen) == "function" then
            SetBlackScreen(v)
        end
    end
})


-- [[ RUNSERVICE LOOP ]]
RunService.Heartbeat:Connect(function()
    -- Update status karakter & kursi tiap frame
    local Char = LP.Character
    local Hum = Char and Char:FindFirstChildOfClass("Humanoid")
    local seat = Hum and Hum.SeatPart
    
    -- logic vehicle boost lo taruh di sini (persis punya lo yang lama)
    if _G.BoostEnabled and seat then
        local vehicle = seat:FindFirstAncestorOfClass("Model")
        if vehicle and vehicle.PrimaryPart then
            if tick() - lastBoostTime >= (_G.Interval or 0.5) then
                local currentVel = vehicle.PrimaryPart.AssemblyLinearVelocity
                local boostDirection = -vehicle.PrimaryPart.CFrame.LookVector

                if _G.StraightBoost then
                    boostDirection = Vector3.new(boostDirection.X, 0, boostDirection.Z).Unit
                end
                
                if currentVel.Magnitude < (_G.MaxForce or 500) then
                    vehicle.PrimaryPart.AssemblyLinearVelocity = currentVel + (boostDirection * (_G.Increment or 50))
                end
                lastBoostTime = tick()
            end
        end
    end
end)

-- [[ MISC ENGINE ]] --
RunService.RenderStepped:Connect(function()
    -- Logic Hide All Names
    if _G.HideName then
        for _, p in pairs(game:GetService("Players"):GetPlayers()) do
            if p.Character and p.Character:FindFirstChild("Head") then
                local head = p.Character.Head
                for _, bg in pairs(head:GetChildren()) do
                    if bg:IsA("BillboardGui") then bg.Enabled = false end
                end
            end
        end
    end

    -- Logic Hide Character (Local Only)
    if _G.HideChar then
        if LP.Character and LP.Character.Parent ~= nil then
            LP.Character.Parent = nil 
        end
    end
end)

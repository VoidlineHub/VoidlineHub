-- [[ VOIDLINE HUB | BARISTA ONLY EDITION ]]
local genv = getgenv()
local fenv = getfenv()

-- // ADONIS & NETWORK FIX
loadstring(game:HttpGet('https://raw.githubusercontent.com/LynX99-9/komtolmmek2/refs/heads/main/Adonis'))()
pcall(function()
    game:GetService('CoreGui').RobloxGui['CoreScripts/NetworkPause']:Destroy()
end)

genv.WindUI = nil
local WindUI = loadstring(game:HttpGet("https://raw.githubusercontent.com/Footagesus/WindUI/main/dist/main.lua"))()

-- [[ DATA BINDING ]]
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Players = game:GetService("Players")
local LP = Players.LocalPlayer
local RunService = game:GetService("RunService")

-- // PROMPTS & CFRAMES
local Prompts = {
    Machine = workspace.BaristaJob.Interactions.MachinePart.MachinePart.MachinePrompt,
    Register = workspace.BaristaJob.Interactions.RegisterPart.RegisterPart.RegisterPrompt,
    Supply = workspace.BaristaJob.Interactions.SupplyPart.SupplyPart.SupplyPrompt,
    Job = workspace.BaristaJob.Interactions.StartPart.StartPart.JobPrompt
}

local CF = {
    Supply = CFrame.new(-5116.78418, 5.78931046, -670.858887),
    Machine = CFrame.new(-4997.1665, 1.58353043, -795.047607),
    Register = CFrame.new(-4994.06934, 1.30402756, -760.247437),
    Start = CFrame.new(-4989.80078, 5.30382967, -715.013062)
}

-- [[ GLOBAL VARIABLES ]]
_G.AutoFarmBarista = false
LastActivity = tick()

-- [[ UTILITY FUNCTIONS ]]
local function GetBaristaElements()
    local Gui = LP.PlayerGui:FindFirstChild("BaristaGUI")
    if Gui then
        local OrderText = Gui:FindFirstChild("StatusFrame") and Gui.StatusFrame:FindFirstChild("OrderText")
        local Minigame = Gui:FindFirstChild("MinigameFrame")
        return Gui, OrderText, Minigame
    end
    return nil, nil, nil
end

local function GetRemote(name)
    for _, v in pairs(game:GetDescendants()) do
        if v:IsA("RemoteEvent") and v.Name == name then return v end
    end
    return nil
end

local function BypassTP(targetCF)
    local Char = LP.Character or LP.CharacterAdded:Wait()
    local Hum = Char:WaitForChild("Humanoid")
    local Root = Char:WaitForChild("HumanoidRootPart")
    
    if Hum and Root then
        Hum.Sit = true
        task.wait(0.5)
        Root.CFrame = targetCF
        task.wait(0.3)
        Hum.Sit = false
    end
end

local function ExecuteStartSequence()
    local tr = GetRemote("TeamChangeRequest")
    if LP.Team and LP.Team.Name ~= "Barista" and tr then
        tr:FireServer("Barista", 11378976, 0, 0, "Detector")
        task.wait(2.5)
    end
    
    BypassTP(CF.Start)
    task.wait(0.8)
    if Prompts.Job and Prompts.Job.Enabled then
        fireproximityprompt(Prompts.Job)
    end
    LastActivity = tick()
end

-- [[ MAIN FARMING LOOP ]]
task.spawn(function()
    while task.wait(0.6) do
        if _G.AutoFarmBarista then
            local _, OrderTextLabel, MinigameFrame = GetBaristaElements()
            
            -- Cek 4 Menit Tanpa Aktivitas
            if tick() - LastActivity >= 240 then
                ExecuteStartSequence()
            end

            if OrderTextLabel then
                local txt = OrderTextLabel.Text:lower()
                local isBroken = (OrderTextLabel.TextColor3.R > 0.8 and (txt:find("break") or txt:find("down")))
                
                if isBroken then
                    BypassTP(CF.Supply)
                    task.wait(0.5)
                    fireproximityprompt(Prompts.Supply)
                    LastActivity = tick()
                    task.wait(0.5)
                    BypassTP(CF.Machine)
                    fireproximityprompt(Prompts.Machine)
                elseif Prompts.Machine.Enabled and not (MinigameFrame and MinigameFrame.Visible) then
                    BypassTP(CF.Machine)
                    fireproximityprompt(Prompts.Machine)
                    LastActivity = tick()
                    repeat task.wait(0.5)
                        LastActivity = tick()
                    until not (MinigameFrame and MinigameFrame.Visible) or not _G.AutoFarmBarista
                elseif Prompts.Register.Enabled and not (MinigameFrame and MinigameFrame.Visible) then
                    task.wait(0.5)
                    BypassTP(CF.Register)
                    task.wait(0.5)
                    fireproximityprompt(Prompts.Register)
                    LastActivity = tick()
                end
            end
        end
    end
end)

-- Persistent Fixes (Minigame Exploit)
RunService.Heartbeat:Connect(function()
    local _, _, MinigameFrame = GetBaristaElements()
    if _G.AutoFarmBarista and MinigameFrame and MinigameFrame.Visible then
        local tz = MinigameFrame:FindFirstChild("TargetZone", true)
        if tz then 
            tz.Size = UDim2.new(1, 0, 1, 0)
            tz.Position = UDim2.new(0, 0, 0, 0) 
        end
    end
end)

-- [[ WINDUI WINDOW ]]
local Window = WindUI:CreateWindow({
    Title = "voidlinehub",
    Icon = "github", 
    Author = "bilzz",
    Folder = "Voidline_Barista",
    Transparent = true,
    Size = UDim2.fromOffset(300, 380),
    OpenButton = {
        Enabled = true,
        Title = "Barista Simulator",
        Draggable = true,
    }
})

-- Home Tab
local HomeTab = Window:Tab({ Title = "Home", Icon = "house" })
HomeTab:Section({ Title = "Update Log" })
HomeTab:Button({
    Title = "Version 1.0 (Lite)",
    Desc = "[+] Focused Barista Autofarm\n[+] Removed Courier & Webhook\n[+] Original Bypass TP",
    Icon = "file-text"
})

-- Autofarm Tab
local AutofarmTab = Window:Tab({ Title = "Autofarm", Icon = "coffee" })
AutofarmTab:Section({ Title = "Barista Work" })

AutofarmTab:Toggle({
    Title = "Autofarm Barista",
    Desc = "Semua otomatis, gaji lancar jaya!",
    Callback = function(state)
        _G.AutoFarmBarista = state 
        if state then
            LastActivity = tick()
            task.spawn(ExecuteStartSequence)
            WindUI:Notify({ Title = "Voidline", Content = "Barista Farm Aktif!" })
        end
    end
})

-- Social Tab
local SocialTab = Window:Tab({ Title = "Social", Icon = "share-2" })
SocialTab:Button({
    Title = "Copy Tiktok Link",
    Icon = "music",
    Callback = function()
        setclipboard("tiktok.com/@billzzhub")
        WindUI:Notify({ Title = "Success", Content = "Link copied!" })
    end
})

WindUI:Notify({ Title = "Voidline Hub", Content = "Barista Edition Loaded!" })


--// load rayfield
local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

--// variabel
local VIM = game:GetService("VirtualInputManager")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")
local TeleportService = game:GetService("TeleportService")
local lp = game.Players.LocalPlayer

_G.IsAutoFarm = false
_G.IsAutoGear = true
_G.AutoRejoinLimit = true
_G.AutoStartOnLoad = false
_G.LimitTimer = 0 
_G.Running = false 

_G.SelectedMotor = "Yamahax-MioSporty"
_G.MotorOptions = {"Yamahax-MioSporty"}

local START_CF = CFrame.new(-8909.42871, 2.39137, 758.84540, -0.8551978469, 0.0000000440, -0.5183017254, 0.0000000650, 1.0000000000, -0.0000000224, 0.5183017254, -0.0000000528, -0.8551978469)

--// LOGIC REJOIN (Limit Detector)
task.spawn(function()
    while task.wait(1) do
        if _G.IsAutoFarm and _G.AutoRejoinLimit then
            local timerGui = lp.PlayerGui:FindFirstChild("TimerGui")
            
            -- Jika AutoFarm ON tapi TimerGui.Enabled TIDAK PERNAH True (artinya event Start ga masuk/Limit)
            if timerGui and timerGui.Enabled == false then
                _G.LimitTimer = _G.LimitTimer + 1
            else
                _G.LimitTimer = 0 -- Reset kalo balapan jalan (Enabled = true)
            end

            -- 180 detik = 3 Menit stuck
            if _G.LimitTimer >= 180 then
                _G.LimitTimer = 0
                TeleportService:Teleport(game.PlaceId, lp)
            end
        end
    end
end)

--// garage logic
local function getGarageData()
    local newList = {}
    pcall(function()
        local data = ReplicatedStorage.DealershipEvents.InitializeCarData:InvokeServer()
        if data and type(data) == "table" then
            for _, info in pairs(data) do
                if info.Name then table.insert(newList, info.Name) end
            end
        end
    end)
    if #newList == 0 then table.insert(newList, "Yamahax-MioSporty") end
    _G.MotorOptions = newList
    return newList
end

--// main logic
local function startMatrixRace()
    if _G.Running then return end
    _G.Running = true
    
    while _G.IsAutoFarm do
        local char = lp.Character
        local hum = char and char:FindFirstChildOfClass("Humanoid")
        if not char or not hum then break end

        -- Hapus pembatas/ban
        pcall(function()
            if workspace.BUILDING_KARTASURA.DRAG_RACE.ACC:FindFirstChild("BAN") then
                workspace.BUILDING_KARTASURA.DRAG_RACE.ACC.BAN:Destroy()
            end
        end)

        -- Setup Posisi
        hum.Sit = true
        task.wait(2)
        char:PivotTo(START_CF)
        task.wait(2)
        
        -- Spawn Motor
        ReplicatedStorage.SpawnCarEvents.SpawnCar:FireServer(_G.SelectedMotor)
        task.wait(7) 

        local targetVehicle = nil
        for _, child in pairs(workspace:GetChildren()) do
            if child:IsA("Model") and string.find(child.Name, "Montors") and string.find(child.Name, lp.Name) then
                targetVehicle = child
                break
            end
        end

        if targetVehicle then
            local driveSeat = targetVehicle:FindFirstChild("DriveSeat")
            if driveSeat then
                char:PivotTo(driveSeat.CFrame)
                task.wait(0.5)
                local prompt = driveSeat:FindFirstChildOfClass("ProximityPrompt")
                if prompt then fireproximityprompt(prompt) end
            end
            task.wait(2)

            -- Start Race
            if game:GetService("TextChatService").ChatVersion == Enum.ChatVersion.TextChatService then
                game:GetService("TextChatService").TextChannels.RBXGeneral:SendAsync("!START")
            else
                ReplicatedStorage.DefaultChatSystemChatEvents.SayMessageRequest:FireServer("!START", "All")
            end
            task.wait(6)

            -- Masuk Gigi 1 (W + P + E)
            VIM:SendKeyEvent(true, Enum.KeyCode.W, false, game)
            task.wait(0.2)
            VIM:SendKeyEvent(true, Enum.KeyCode.P, false, game)
            task.wait(0.1)
            VIM:SendKeyEvent(false, Enum.KeyCode.P, false, game)
            task.wait(0.1)
            VIM:SendKeyEvent(true, Enum.KeyCode.E, false, game)
            task.wait(0.05)
            VIM:SendKeyEvent(false, Enum.KeyCode.E, false, game)

            local raceActive = true
            local pGui = lp.PlayerGui
            local timerGui = pGui:WaitForChild("TimerGui", 10)
            local timerValue = timerGui:WaitForChild("Frame"):WaitForChild("TimeValue")

            -- Auto Shifter Logic
            local gearLoop = task.spawn(function()
                if not _G.IsAutoGear then return end
                task.wait(3) -- Gigi 2
                if raceActive then
                    VIM:SendKeyEvent(true, Enum.KeyCode.E, false, game)
                    task.wait(0.05)
                    VIM:SendKeyEvent(false, Enum.KeyCode.E, false, game)
                end
                while raceActive do
                    task.wait(4) -- Gigi seterusnya
                    if raceActive then
                        VIM:SendKeyEvent(true, Enum.KeyCode.E, false, game)
                        task.wait(0.05)
                        VIM:SendKeyEvent(false, Enum.KeyCode.E, false, game)
                    end
                end
            end)

            -- Monitor Selesai
            local connection
            connection = RunService.Heartbeat:Connect(function()
                if not _G.IsAutoFarm or not targetVehicle.Parent or timerGui.Enabled == false then 
                    raceActive = false
                    if connection then connection:Disconnect() end
                    return 
                end
                -- Berhenti kalau warna teks berubah jadi Lime (Finish)
                if timerValue.TextColor3.G > 0.8 and timerValue.TextColor3.B < 0.3 then 
                    raceActive = false
                    if connection then connection:Disconnect() end
                end
            end)

            local timeout = 0
            while raceActive and timeout < 35 do
                task.wait(0.5)
                timeout = timeout + 0.5
            end
            
            raceActive = false
            if connection then connection:Disconnect() end
            task.cancel(gearLoop)

            -- Berhenti tekan gas & hapus motor lama
            VIM:SendKeyEvent(false, Enum.KeyCode.W, false, game)
            task.wait(1)
        end
        if not _G.IsAutoFarm then break end
        task.wait(3) -- Jeda antar balapan
    end
    _G.Running = false
end

--// GUI Rayfield
local Window = Rayfield:CreateWindow({
   Name = "VoidlineHub",
   LoadingTitle = "VoidlineHub",
   LoadingSubtitle = "by Bilzz",
   ConfigurationSaving = {
      Enabled = true,
      FolderName = "VoidlineHub_Configs",
      FileName = "EliteDrag_Save"
   }
})

local MainTab = Window:CreateTab("Main Drag", 4483362458)

MainTab:CreateSection("Garage")
local MotorDropdown = MainTab:CreateDropdown({
   Name = "Select Motor",
   Options = _G.MotorOptions,
   CurrentOption = {_G.SelectedMotor},
   Flag = "MotorFlag",
   Callback = function(Option) _G.SelectedMotor = Option[1] end,
})

MainTab:CreateButton({
   Name = "Refresh Motor",
   Callback = function()
      local data = getGarageData()
      MotorDropdown:Refresh(data, true)
   end,
})

MainTab:CreateSection("Auto Race")
MainTab:CreateToggle({
   Name = "Auto Shifter",
   CurrentValue = _G.IsAutoGear,
   Flag = "GearFlag",
   Callback = function(v) _G.IsAutoGear = v end,
})

MainTab:CreateToggle({
   Name = "Main Auto Farm",
   CurrentValue = _G.IsAutoFarm,
   Flag = "FarmFlag",
   Callback = function(v)
      _G.IsAutoFarm = v
      if _G.IsAutoFarm then task.spawn(startMatrixRace) end
   end,
})

MainTab:CreateSection("Settings")
MainTab:CreateToggle({
   Name = "Auto Start on Load",
   CurrentValue = _G.AutoStartOnLoad,
   Flag = "AutoStartFlag",
   Callback = function(v) _G.AutoStartOnLoad = v end,
})

MainTab:CreateToggle({
   Name = "Auto Rejoin (Anti Limit)",
   CurrentValue = _G.AutoRejoinLimit,
   Flag = "RejoinFlag",
   Callback = function(v) _G.AutoRejoinLimit = v end,
})

Rayfield:LoadConfiguration()

task.spawn(function()
    local data = getGarageData()
    MotorDropdown:Refresh(data, true)
    task.wait(2)
    if _G.AutoStartOnLoad and _G.IsAutoFarm then
        task.spawn(startMatrixRace)
    end
end)

local VelarisUI = loadstring(game:HttpGet("https://raw.githubusercontent.com/nhfudzfsrzggt/brigida/refs/heads/main/dist/main.lua", true))()
local HttpService = game:GetService("HttpService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local TweenService = game:GetService("TweenService")
local Players = game:GetService("Players")
local LP = Players.LocalPlayer -- Kita pakai LP untuk LocalPlayer
local RunService = game:GetService("RunService")
local PlayerData = LP:WaitForChild("PlayerData") -- DIPERBAIKI: Dari Player jadi LP
local UserInputService = game:GetService("UserInputService")



-- [[ GUI VELARIS ]] --
local Window = VelarisUI:Window({
    Title     = "Drag Drive Simulator",
    Footer    = "By Voidline",
    Content   = "Quadra Method Edition",             
    Color     = "Default",            
    Version   = "1.0",
    Image     = "rbxassetid://101833678008843",    
    Size      = UDim2.fromOffset(640, 400),
    ShowUser  = true,                  
    Search    = true,                  
    Animation = true,
})

local FarmTab = Window:AddTab({ Name = "AutoFarm", Icon = "Play" })
local FarmSection = FarmTab:AddSection({ Title = "Mandalika Farm", Open = true })

FarmSection:AddToggle({
    Title    = "On Autorace",
    Content  = "Dynamic Speed + Double Tap",
    Default  = false,
    Callback = function(value)
        _G.AutoRace = value
    end
})

FarmSection:AddToggle({
    Title    = "Anti-AFK Ultra",
    Content  = "Fix Error 278",
    Default  = false,
    Callback = function(value)
        _G.AntiAFK = value
        
        if value then
            print("VoidlineHub | Anti-AFK Ultra Active")
            task.spawn(function()
                while _G.AntiAFK do
                    -- 1. Simulasi Input Keyboard (Biar dikira mencet tombol)
                    VirtualUser:CaptureController()
                    VirtualUser:Button1Down(Vector2.new(0, 100), workspace.CurrentCamera.CFrame)
                    task.wait(0.2)
                    VirtualUser:Button1Up(Vector2.new(0, 100), workspace.CurrentCamera.CFrame)
                    
                    -- 2. Gerakin Kamera dikit (Sangat efektif lawan idle 20 menit)
                    local cam = workspace.CurrentCamera
                    cam.CFrame = cam.CFrame * CFrame.Angles(0, math.rad(0.1), 0)
                    task.wait(0.1)
                    cam.CFrame = cam.CFrame * CFrame.Angles(0, math.rad(-0.1), 0)

                    -- 3. Jeda Random (Biar gak kebaca bot pola tetap)
                    task.wait(math.random(30, 60)) 
                end
            end)
        end
    end
})
